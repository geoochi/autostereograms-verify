{
  "packageManager": "pnpm@10.17.0",
  "name": "stereo-verify",
  "type": "module",
  "scripts": {
    "approve-builds": "pnpm approve-builds",
    "build": "echo 'No build needed'",
    "vercel-build": "pnpm approve-builds && pnpm run build"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.81"
  },
  "engines": {
    "node": ">=22"
  }
}
